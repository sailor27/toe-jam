import defaultState from './../constants/DefaultState.js';

export default(state  = defaultState, action) => {
  switch (action.type) {
    case 'CHANGE_HISTORY':
    //how to change the history goes here
  }
 }
};
